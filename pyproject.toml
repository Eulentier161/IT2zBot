[tool.poetry]
name = "it2zbot"
version = "0.1.0"
description = ""
authors = ["Eulentier161"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.9"
discord = "^1.7.3"
pyaml = "^21.10.1"
python-dotenv = "^0.20.0"
uvloop = "^0.16.0"

[tool.poetry.dev-dependencies]
black = "^22.3.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
